[ -z "$HOSTNAME" ] && HOSTNAME=`uname -n`
[ -z "$DOMAINNAME" ] && DOMAINNAME=`domainname -d`
#if [ $(ls $HOME/.ssh/*.pub) ]
#then
#ID_KEYS=''
  #for i in $HOME/.ssh/*.pub
  #do
    #PUBKEY=$(basename $i)
    #KEY=" ${PUBKEY%.pub}"
    #ID_KEYS+=${KEY}
  #done
#/usr/bin/keychain ${ID_KEYS}
#fi
#[ -f $HOME/.keychain/$HOSTNAME-sh ] && . $HOME/.keychain/$HOSTNAME-sh
#[ -f $HOME/.keychain/$HOSTNAME-sh-gpg ] && . $HOME/.keychain/$HOSTNAME-sh-gpg # для GPG-ключей
